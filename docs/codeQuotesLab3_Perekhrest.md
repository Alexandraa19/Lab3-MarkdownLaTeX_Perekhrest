# Блоки кода и цитаты

## Встроенный (inline) код

Команда `git status` показывает состояние репозитория.

Команда `git add .` добавляет все файлы в staging area.

## Блок кода на Python

```python
print("Hello, Markdown!")
```

## Блок кода на C#

```csharp
string name;
name = Console.ReadLine();
Console.WriteLine(name);
```

## Блок кода без указания языка

```
Это блок кода без указания языка.
Он отображается моноширинным шрифтом.
```

## Цитаты

> Это цитата в Markdown.

> Первый уровень
>> Второй уровень